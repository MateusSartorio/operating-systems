#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>
#include <wait.h>

int main(int argc, char** argv, char** envp) {
    pid_t pid1 = fork();
    pid_t pid2 = fork();

    if(pd1 && pid2) {
        printf("Pro  ")
    }


    return 0;
}
